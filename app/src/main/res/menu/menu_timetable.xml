<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menu_back"
        android:title="@string/go_back"
        android:icon="@drawable/ic_navigate_before_24dp"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/menu_default_node"
        android:title="@string/default_node"
        android:icon="@drawable/ic_home_24dp"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/menu_pick_node"
        android:title="@string/pick_node"
        android:icon="@drawable/ic_list_24dp"
        app:showAsAction="ifRoom" />
</menu>